library package_releases;

class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;

  /// Returns [value] plus 2.
  int addTwo(int value) => value + 2;

  /// Returns [value] plus 2.
  int addThree(int value) => value + 3;

  /// Returns [value] plus 2.
  int addFour(int value) => value + 4;
}
